import 'package:flutter/material.dart';

const primaryColor = Color.fromRGBO(242, 174, 47, 1);

const darkColor = Color.fromRGBO(32, 32, 32, 1);

const greyColor = Color.fromRGBO(158, 162, 173, 1);

const greyColor1 = Color.fromRGBO(228, 228, 228, 1);

const online = Color(0xFF4BCB1F);

ValueNotifier<Locale> locale = ValueNotifier(Locale('en', 'US'));

// backgrounds toast
const colorBgToastError = Color(0xfff80202);
const colorBgToastSuccessBg = Color(0xff1be505);
const colorBgToastMsgBg = Color(0xFFCEAF0A);
